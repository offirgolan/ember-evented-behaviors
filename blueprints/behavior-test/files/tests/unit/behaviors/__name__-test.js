import Ember from 'ember';
import { module, test } from 'qunit';

moduleFor('behavior:<%= dasherizedModuleName %>', 'Unit | Behavior | <%= dasherizedModuleName %>');

test('it works', function(assert) {
  let behavior = this.subject();
  assert.ok(behavior);
});
