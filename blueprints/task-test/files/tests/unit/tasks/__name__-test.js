import Ember from 'ember';
import { module, test } from 'qunit';

moduleFor('task:<%= dasherizedModuleName %>', 'Unit | Task | <%= dasherizedModuleName %>');

test('it works', function(assert) {
  let task = this.subject();
  assert.ok(task);
});
